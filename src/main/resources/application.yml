spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/security00?useUnicode=true&character_set_server=utf8mb4&useSSL=false&serverTimezone=Hongkong
    username: root
    password: 123456
logging:
  level:
    root: info
    org:
      springframework:
        security: DEBUG
        web: ERROR
    com:
      notejava:
        mapper: debug
mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml